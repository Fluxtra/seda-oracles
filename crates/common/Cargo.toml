[package]
name = "common"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib"]

[dependencies]
anyhow = "1.0"
seda-sdk-rs = "1.2"
serde = { version = "1.0", default-features = false, features = ["derive"] }
serde_json = "1.0"
